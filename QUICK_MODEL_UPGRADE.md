# 🎨 快速提升模型质量的三种方案

## 方案一：下载免费模型（推荐，效果最好）

### 步骤：
1. **访问 Sketchfab**：https://sketchfab.com
2. **搜索模型**：
   - 搜索 "low poly pig gltf"
   - 筛选：Free + Downloadable
3. **下载 GLB 文件**
4. **放到项目**：`public/assets/models/pig.glb`
5. **启用外部模型**：修改 `AnimalModels.js` 第 6 行：
   ```javascript
   static useExternalModels = true; // 改为 true
   ```

### 推荐搜索关键词：
- `low poly pig gltf`
- `cute rabbit 3d model`
- `stylized cat glb`
- `game character dog`

---

## 方案二：使用在线模型 URL（最快测试）

如果你找到了公开的 GLTF 模型 URL，可以直接使用：

修改 `ModelLoader.js`，添加在线模型 URL：

```javascript
const onlineModels = {
    pig: 'https://example.com/models/pig.glb',
    rabbit: 'https://example.com/models/rabbit.glb',
    // ...
};
```

---

## 方案三：改进代码生成的模型（无需下载）

如果暂时不想下载模型，我可以帮你：
1. 使用更复杂的几何体组合
2. 添加纹理贴图（让模型有"皮肤"质感）
3. 使用 Three.js 的 BufferGeometry 创建更精细的形状

---

## 🚀 快速开始（推荐方案一）

1. **创建模型目录**：
   ```bash
   mkdir -p public/assets/models
   ```

2. **去 Sketchfab 下载 4 个模型**（每个 < 2MB）

3. **重命名并放入**：
   - `public/assets/models/pig.glb`
   - `public/assets/models/rabbit.glb`
   - `public/assets/models/cat.glb`
   - `public/assets/models/dog.glb`

4. **启用外部模型加载**（代码已准备好）

5. **刷新游戏** → 模型自动加载！

---

## 💡 小贴士

- **文件大小**：每个模型建议 < 2MB
- **面数**：选择 Low Poly（低多边形），性能更好
- **格式**：优先 GLB（比 GLTF 文件更小）
- **许可**：确认是 CC0 或免费商用许可

需要我帮你实现哪个方案？
